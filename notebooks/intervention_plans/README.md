# notebooks/intervention_plans

**Empty of source files.** CLAUDE.md expects the MOMENT lift-transplant experiment
plan and the B4 forward-looking interventions (trainable Koopman lift, gating hybrid
router, LoRA/fine-tune on ETTh, ERA5/BARRA-R real chaotic weather) here.

Neither exists as a standalone file in Downloads. What was found instead:

- The **MOMENT paper itself** (`arXiv:2402.03885v3`) → `docs/theory_reference/`.
  The transplant *plan* is not a separate document.
- **B4 items** appear only as prose inside `docs/ideas_list.pdf` (ideas 5, 7, and
  the Group-1 threads) and inside the chat transcripts in `docs/chat_transcripts/`.

This folder is a placeholder so the structure matches CLAUDE.md.
